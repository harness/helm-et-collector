apiVersion: v1
kind: Service
metadata:
  name: {{ include "et-collector.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "et-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "et-collector.selectorLabels" . | nindent 4 }}
