apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "et-collector.fullname" . }}-test-connection"
  labels:
    {{- include "et-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "et-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
